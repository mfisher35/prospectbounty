import React, { useState } from 'react';

import styled from 'styled-components';

const HunterInit = ({onHunterInit}) => {
  //const [userData, setUserData] = useState(null);



  const cleanError = (error) => {
  }

  return (
    <div>
    Hunter Init
    </div>
  );
};

export default HunterInit;
